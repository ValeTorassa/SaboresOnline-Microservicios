@model CouponDto

<form asp-action="CouponDelete">
    <br />
    <div class="container border p-3">
        <h1 class="text-white text-center">Delete Coupon</h1>
        <input hidden asp-for="CouponId">
        <hr />
        <div class="row">
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Coupon Code</label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="CouponCode" disabled class="form-control" />
            </div>
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Discount Amount</label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="DiscountAmount" disabled class="form-control" />
            </div>
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Min Amount</label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="MinAmount" disabled class="form-control" />
            </div>

            <div class="col-5">
                <a asp-action="CouponIndex" class="btn btn-primary form-control d-inline-block mr-2">Back to List</a>
            </div>
            <div class="col-5">
                <input type="submit" value="Delete" class="btn btn-danger form-control d-inline-block" />
            </div>

        </div>

        <div>
        </div>
    </div>
</form>


@*
    //asp-action: Especifica el nombre de la acción del controlador que se va a invocar cuando se envía el formulario.
    //asp-controller: Especifica el nombre del controlador que se va a invocar cuando se envía el formulario.
    //asp-for: Enlaza el control de entrada (<input>) a la propiedad CouponCode del modelo.
    //asp-validation-for: Muestra mensajes de validación asociados a la propiedad CouponCode del modelo.
    //asp-append-version: Agrega una cadena de consulta de versión a la ruta de acceso de la solicitud de archivo.
    //asp-area: Especifica el nombre del área que se va a usar para generar la ruta de acceso de la acción.
    //asp-route-id: Especifica el nombre del parámetro de ruta que se va a usar para generar la ruta de acceso de la acción.
    //asp-route: Especifica el nombre de la ruta que se va a usar para generar la ruta de acceso de la acción.
    //asp-all-route-data: Especifica que se van a incluir todos los parámetros de ruta en la ruta de acceso de la acción.
    //asp-fragment: Especifica el fragmento que se va a usar para generar la ruta de acceso de la acción.
    //asp-host: Especifica el nombre del host que se va a usar para generar la ruta de acceso de la acción.
    //asp-protocol: Especifica el protocolo que se va a usar para generar la ruta de acceso de la acción.
    //asp-route-fragment: Especifica el fragmento que se va a usar para generar la ruta de acceso de la acción.
    //asp-route-host: Especifica el nombre del host que se va a usar para generar la ruta de acceso de la acción.
    //asp-route-protocol: Especifica el protocolo que se va a usar para generar la ruta de acceso de la acción.
    //asp-route-asp-route-fragment: Especifica el fragmento que se va a usar para generar la ruta de acceso de la acción.
    //asp-route-asp-route-host: Especifica el nombre del host que se va a usar para generar la ruta de acceso de la acción.
*@

@*
asp-for (Enlace de Datos):
    Vincula un control HTML, como <input> o <select>, a una propiedad específica del modelo.
    Facilita el enlace bidireccional entre la vista y el modelo, permitiendo la entrada y salida de datos.

asp-action (Enlace de Acción):
    Especifica la acción del controlador a la que se enviará una solicitud cuando se interactúe con un formulario.
    para definir la acción de destino para operaciones como enviar datos.

asp-validation-for (Validación):
    Muestra mensajes de validación asociados a una propiedad del modelo.
    Se utiliza para presentar mensajes de error de validación en la interfaz de usuario
*@