@model LoginRequestDto

<form method="post">
    <div class="container border p-4">
        <div class="row text-center">
            <div class="col-12">
                <h1>Login</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-12 col-md-6 offset-3 pb-2">
                <input asp-for="UserName" class="form-control" placeholder="Username..."></input>
                <span asp-validation-for="UserName" class="text-danger" />
            </div>
            <div class="col-12 col-md-6 offset-3 pb-2">
                <input asp-for="Password" class="form-control" placeholder="Password..."></input>
                <span asp-validation-for="Password" class="text-danger" />
            </div>
            <div class="col-12 col-md-6 offset-3 pb-2">
                <button type="submit" class="form-control btn btn-success" value="Submit">Login</button>
            </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}